import { NgModule } from '@angular/core';

@NgModule({
  declarations: [
    // Remove standalone components from declarations
  ],
  imports: [
    // Remove imports as they are now in main.ts
  ],
  providers: [
    // Services are now provided in main.ts for standalone bootstrap
  ]
})
export class AppModule { }